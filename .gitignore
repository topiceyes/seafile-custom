# ============ 本地调试数据（不入库）============
# 运行时卷：MariaDB 库文件、用户上传的文件实体、日志、已渲染的配置
deploy/seafile-data/
deploy/seafile-mysql-db/

# ============ 含真实密钥的本地配置（模板见 deploy/.env.example）============
deploy/.env

# ============ 上游源码克隆 ============
# seahub/ 与 seafile/ 各自是独立 git 仓库（上游 haiwen/*），体积大，不入库。
# 二开改动全部通过 patches/ 发布：CI 按 patches/MANIFEST.md 的固定 SHA 拉上游，
# 再 git am patches/*.patch（见 docs/010）。
seafile/
seahub/

# ============ 系统 / 编辑器 / 工具 ============
.DS_Store
*.pyc
__pycache__/
.claude/settings.local.json

# 本地彩排数据（见 docs/007 彩排章节）
deploy/rehearsal*/
deploy/.env.rehearsal*
deploy/seafile-mc-devbuild*
