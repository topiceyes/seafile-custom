# Seafile 生产环境部署配置模板
# 用法：cp .env.prod.example .env && 填值。.env 不入库（.gitignore 已排除）。
# 完整部署流程见 docs/007-production-deployment.md。
COMPOSE_FILE='seafile-prod.yml'

# 站点域名（纯域名，不带协议/端口）。填【用户在浏览器里输入的那个域名】——
# 它同时决定 nginx server_name、SERVICE_URL 和钉钉回调地址。反代模式下不是容器地址。
SEAFILE_DOMAIN='seafile.example.com'

# ---- TLS 终止位置：两种模式二选一（详见 docs/007 §9）----
#
#   A) 容器自己签 Let's Encrypt 证书（需 DNS 指向本机 + 80 端口公网可达）
#      SEAFILE_SERVER_LETSENCRYPT='true'
#      SEAFILE_SERVER_PROTOCOL='https'
#
#   B) 上游云反向代理终止 TLS，容器只监听 80  ← 本项目生产实际形态
#      证书装在代理上；代理用明文 http 转发到本机 80 端口。
#      ⚠️ SEAFILE_SERVER_PROTOCOL 必须显式设 https：它决定 SERVICE_URL /
#         FILE_SERVER_ROOT 的协议，不设会生成 http:// 链接，导致文件上传下载、
#         分享链接、钉钉回调地址全错（而页面本身还能打开，很难查）。
#      另注：代理侧要放开请求体大小限制（本容器 location / 已设 client_max_body_size 0）
SEAFILE_SERVER_LETSENCRYPT='false'
SEAFILE_SERVER_PROTOCOL='https'

# 自建镜像。由 GitHub Actions 构建并推送到 ghcr.io（见 docs/010），
# tag 形如 12.0.14-dingtalk.<补丁数>.<构建输入哈希>，每次构建出的 digest
# 记在 docs/010 §9 台账里。
# 更新：改这里的 tag → docker compose pull && docker compose up -d
# 仓库与镜像包都是 public，拉取不需要 docker login。
# 默认用 digest，不用 tag。tag 现在已带构建输入哈希（12.0.14-dingtalk.<N>.<hash>，
# 见 docs/010 §4），同 tag 基本等价于同内容；但 digest 是唯一不依赖任何命名约定的
# 保障 —— 上游若重推基础镜像的同一 tag，本仓库输入没变而镜像内容会变。
# digest 从 CI 的 run summary 或 docs/010 §9 台账取。
SEAFILE_PRO_IMAGE='ghcr.io/topiceyes/seafile-mc@sha256:624c439c3d6bc9ac4bdb28817fe06e52d0101fc2d728e5a1e2fa5454469e0cb9'
# 图省事也可以先用 tag（更新时改这一行）——它含构建输入哈希，正常不会漂移：
# SEAFILE_PRO_IMAGE='ghcr.io/topiceyes/seafile-mc:12.0.14-dingtalk.9.4edcb25d'
#
# ⚠️ 走【离线导入】（服务器连不上镜像仓库，deploy/make-offline-bundle.sh）时，
#    这一行必须用上面的 tag 形式，不能用 digest 形式。原因：docker save 一个
#    按 digest 拉下来的镜像会写出 "RepoTags": null，docker load 后它是无标签的
#    悬空镜像，compose 按 repo@sha256:… 找不到，于是又去联网拉、又失败。
#    载入后可用 `docker inspect --format '{{index .RepoDigests 0}}' <tag>` 核对
#    digest 仍是 sha256:624c439c…4469e0cb9（tag 带构建输入哈希，内容不会变）。

# 数据持久化目录（服务器绝对路径，磁盘要够大：库 + 上传文件 + 备份都在这）
SEAFILE_VOLUME='/data/seafile'
SEAFILE_MYSQL_VOLUME='/data/seafile-mysql'

# MariaDB
# 生成：openssl rand -hex 16
SEAFILE_MYSQL_ROOT_PASSWORD='change-me'
SEAFILE_MYSQL_DB_HOST='db'
SEAFILE_MYSQL_DB_USER='seafile'
SEAFILE_MYSQL_DB_PASSWORD='change-me'

# Seafile 12 内部服务 JWT 认证密钥
# 生成：openssl rand -base64 48
JWT_PRIVATE_KEY=''

# 首次启动自动创建的管理员（仅首次生效；之后用 reset-admin.sh 改）
SEAFILE_ADMIN_EMAIL='admin@example.com'
SEAFILE_ADMIN_PASSWORD='change-me'

# ---- 以下由 init-conf.sh --prod 在首启完成后追加进 seahub_settings.py ----
# （SECRET_KEY 与数据库配置由首启 setup 自动生成，无需预置）
# 钉钉企业内部应用凭据（钉钉开发者后台 → 应用 → 凭证与基础信息）
SEAHUB_DINGTALK_APP_KEY=''
SEAHUB_DINGTALK_APP_SECRET=''
